
<div class="user new">
	<h2>Create User</h2>

	<%= form_for(:user, :html => { :multipart => true }, :url => {:action => 'create'}) do |f| %>

		<%= render(:partial => 'form', :locals => {:f => f}) %>

		<%= submit_tag('Create User') %>

		<% end %>

</div>
