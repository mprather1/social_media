# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/

#refreshComments = ->
#  $('#cmt').data('source')
#  outputElement = document.querySelector("#comment-list")
#  outputElement.innerHTML = '<%= escape_javascript(render: @page.comments) %>'
#  $('#comment-list').load('<%= escape_javascript(render: "#{cmt}") %>')
#setInterval(refreshComments, 5000)

#$('#comment-list').onload = "setTimeout('auto_reload()', 10000")
#timedRefresh = (timeoutPeriod) ->
#  setTimeout("('#comment-list').reload(true)" timeoutPeriod)

#timedRefresh(1000)
